{
  "name": "persistjs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r build",
    "build:prod": "pnpm -r build:prod",
    "test": "pnpm -r test",
    "documentation": "pnpx astro dev --root ./documentation",
    "shipit": "pnpm build:prod && lerna version && pnpm publish -r"
  },
  "keywords": [],
  "author": "Gustav Eikaas",
  "license": "ISC",
  "devDependencies": {
    "conventional-changelog-conventionalcommits": "^5.0.0",
    "lerna": "^6.4.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
